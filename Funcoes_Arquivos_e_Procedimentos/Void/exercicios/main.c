#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int x, y, z;
	
	printf("Informe 3 numeros em sequencia:\n ");
	scanf("%d", &x);
	scanf("%d", &y);
	scanf("%d", &z);
	
	
	
	return 0;
}
