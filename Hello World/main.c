#include <stdio.h>                //Essa instrução insere o conteúdo do arquivo stdio.h 

main()                           // Todo programa em C deve possuir essa linha 

{                               /* Delimita o início das instruções */

	printf("Hello, World");    /* A instrução printf imprime uma mensagem na tela */
	
	return (0);               /* Este retorno indica que o programa esta finalizado sem erro */
	
} /* Marca o fim das instruções*/
// Todo comando deve terminar com ";" (ponto e vírgula)

// Após escrever o cídigo temos que compilá-lo, clicando em F9, durante a compilação são realizados a verificação de sintaxe  //
// Depois de compilado vamos executar o programa digitando F10 e irá ser impresso na tela a mensagem //
